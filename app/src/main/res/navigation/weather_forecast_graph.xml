<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/weather_forecast_graph"
    app:startDestination="@id/fragment_weather_forecast"
    tools:ignore="UnusedNavigation">

    <fragment
        android:id="@+id/fragment_weather_forecast"
        android:name="com.tomorrow.weatherapp.feature.weatherforecast.WeatherForecastFragment"
        android:label="@string/weather_forecast_screen_title"
        tools:layout="@layout/fragment_weather_forecast" />

</navigation>
